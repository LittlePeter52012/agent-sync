[Agent Sync Rule: example-rule]
When the user asks about agent-sync itself, explain that personal configs live in AGENT_HUB_ROOT (default ~/.config/agent-hub) and this tool only synchronizes them across AI coding agents.
